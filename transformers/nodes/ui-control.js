module.exports = function (node, baseId, themeId) {
    node.type = 'ui-control'

    // update properties
    // NONE

    // new properties
    node.ui=baseId;

    // remove properties
    // NONE

    return node
}
